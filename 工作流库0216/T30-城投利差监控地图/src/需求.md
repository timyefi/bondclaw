注意取数数据库，_cursor对应 bond 配置, _cursor_pg对应 tsdb_pg配置

import numpy as np import pandas as pd import itertools  bd = \_setting['dt']['startDT'] ed = \_setting['dt']['endDT'] target\_term = str(\_setting['target\_term'])   bd1=pd.to\_datetime(bd) - pd.DateOffset(days=30)  bd1=pd.read\_sql(f"""SELECT min(dt) as 日期 FROM hzcurve\_credit                         WHERE target\_term=2 and dt >= '{bd1}'""",\_cursor\_pg) bd1=bd1['日期'].iloc[0].strftime('%Y-%m-%d') trade\_dates=pd.read\_sql(f"""SELECT max(dt),min(dt) FROM hzcurve\_credit                         WHERE target\_term=2 and dt <= '{ed}' AND dt >= '{bd}'""",\_cursor\_pg) trade\_dates1=pd.read\_sql(f"""SELECT Distinct dt as 日期 FROM hzcurve\_credit                         WHERE target\_term=2 and dt <= '{ed}' AND dt >= '{bd}'""",\_cursor\_pg) trade\_dates1['日期']=pd.to\_datetime(trade\_dates1['日期']) result=pd.DataFrame() trade\_dates=trade\_dates.iloc[0,:].apply(lambda x: x.strftime('%Y-%m-%d')).tolist() unique\_dates = "','".join(x for x in trade\_dates) trade\_dates1=trade\_dates1['日期'].dt.strftime('%Y-%m-%d').tolist() unique\_dates1 = "','".join(x for x in trade\_dates1) date\_e=max(trade\_dates) date\_b=min(trade\_dates)  query=f""" SELECT  分类, round(((sum(收益率4)-sum(收益率1))\*100)::numeric,2) as 近7天收益率变化bp, round(((sum(收益率4)-sum(收益率2))\*100)::numeric,2) as 近1个月收益率变化bp, round(((sum(收益率4)-sum(收益率3))\*100)::numeric,2) as "{date\_b}-{date\_e}期间收益率变化bp", round(((sum(收益率4)))::numeric,2) as "当前收益率%%" FROM (     SELECT 分类,收益率1,收益率2,收益率3,收益率4 FROM           (SELECT         A.DT,          C.省 as 分类,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (PARTITION BY C.省 ORDER BY A.dt RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW) AS 收益率1,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (PARTITION BY C.省 ORDER BY A.dt RANGE BETWEEN INTERVAL '30 days' PRECEDING AND CURRENT ROW) AS 收益率2,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (PARTITION BY C.省 ORDER BY A.dt) AS 收益率3,         LAST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (PARTITION BY C.省 ORDER BY A.dt) AS 收益率4         FROM hzcurve\_credit A         LEFT JOIN (SELECT \* FROM tc最新所属曲线 WHERE 日期 = (SELECT MAX(日期) FROM tc最新所属曲线)) D         ON A.trade\_code=D.trade\_code         LEFT JOIN 曲线代码 B         ON D.代码=B.代码         LEFT JOIN basicinfo\_xzqh\_ct C         ON B.子类=C.城投区域         WHERE          A.dt >='{date\_b}'         and B.大类='城投'         AND A.target\_term='{target\_term}'         AND A.balance>0         GROUP BY          分类,A.dt         UNION     SELECT          A.dt,         '全国' as 分类,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (ORDER BY A.dt RANGE BETWEEN INTERVAL '7 days' PRECEDING AND CURRENT ROW) AS 收益率1,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (ORDER BY A.dt RANGE BETWEEN INTERVAL '30 days' PRECEDING AND CURRENT ROW) AS 收益率2,         FIRST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (ORDER BY A.dt) AS 收益率3,         LAST\_VALUE(SUM(A.stdyield\*A.balance)/SUM(A.balance)) OVER (ORDER BY A.dt) AS 收益率4                FROM hzcurve\_credit A         LEFT JOIN (SELECT \* FROM tc最新所属曲线 WHERE 日期 = (SELECT MAX(日期) FROM tc最新所属曲线)) D         ON A.trade\_code=D.trade\_code         LEFT JOIN 曲线代码 B         ON D.代码=B.代码         LEFT JOIN basicinfo\_xzqh\_ct C         ON B.子类=C.城投区域     WHERE          A.dt >='{date\_b}'         and B.大类='城投'         AND A.target\_term='{target\_term}'         AND A.balance>0     GROUP BY A.dt         ) SQ     WHERE SQ.DT='{date\_e}' ) AS subquery group by 分类 ORDER BY "{date\_b}-{date\_e}期间收益率变化bp" ASC; """  result = pd.read\_sql(query, \_cursor\_pg)  





将上面的代码，最终生成的result，转换成下面的raw1（省）、raw2（市），要求省、市的格式都完全匹配（有的带市，有的不带等细节），数据结构完全匹配；你需要先充分研究数据格式（取数方法从/data/项目/快速处理/common 中找）：



import numpy as np import pandas as pd from datetime import datetime, date, timedelta, time   dt = \_setting['dt']  trade\_dates=pd.read\_sql(f"""SELECT MAX(DT) AS DT FROM \`stock\`.\`temp\_5843\`                         WHERE DT<= '{dt}' AND DT != '2023-01-03'                          AND DT != '2023-05-10'""",\_cursor).values[0][0] trade\_dates1=pd.read\_sql(f"""SELECT MAX(DT) AS DT FROM \`stock\`.\`temp\_5857\`                         WHERE DT<= '{dt}' AND DT != '2023-01-03'                          AND DT != '2023-05-10'""",\_cursor).values[0][0] if trade\_dates <= trade\_dates1:     ed=trade\_dates.strftime("%Y-%m-%d") else:     ed=trade\_dates1.strftime("%Y-%m-%d")        raw1=pd.read\_sql(f"""SELECT \* FROM \`stock\`.\`temp\_5843\`                   WHERE DT = '{ed}' """,\_cursor) raw1=raw1.groupby(['PROVINCE','DT']).agg({'CLOSE':'mean'}).reset\_index(drop=False) raw1.columns=['name','dt','value'] raw1['name']=raw1['name'].apply(lambda x : str(x)[0:2]) raw1.loc[raw1['name']=='内蒙','name']='内蒙古' raw1.loc[raw1['name']=='黑龙','name']='黑龙江' raw1['value']=raw1['value'].apply(lambda x: round(x\*100,0))  raw2=pd.read\_sql(f"""SELECT \* FROM \`stock\`.\`temp\_5857\`                  WHERE DT = '{ed}'""",\_cursor) raw2=raw2.groupby(['PROVINCE','DT','CITY']).agg({'CLOSE':'mean'}).reset\_index(drop=False) raw2['PROVINCE']=raw2['PROVINCE'].apply(lambda x : str(x)[0:2]) raw2.loc[raw2['PROVINCE']=='内蒙','PROVINCE']='内蒙古' raw2.loc[raw2['PROVINCE']=='黑龙','PROVINCE']='黑龙江'  raw2['CITY']=raw2['CITY'].apply(lambda x:str(x)[0:2]) raw2['CLOSE']=raw2['CLOSE'].apply(lambda x:round(x\*100,0)) title=ed+' 存续城投利差地图'
