# -*- coding: utf-8 -*-
"""
曲线优化配置文件

包含数据库连接参数、期限定义、筛选条件等配置项
"""

import os

# ============================================================================
# 数据库配置
# ============================================================================

DB_HOST = os.environ.get('DB_HOST', 'localhost')
DB_PORT = int(os.environ.get('DB_PORT', 3306))
DB_USER = os.environ.get('DB_USER', 'root')
DB_PASSWORD = os.environ.get('DB_PASSWORD', '')
DB_NAME = os.environ.get('DB_NAME', 'bond_db')

# ============================================================================
# 标准期限点定义（中债曲线18个标准期限点）
# ============================================================================

STANDARD_TERMS = [
    0,           # 0年
    0.083333333, # 1个月
    0.24999999899999997, # 3个月
    0.49999999799999995, # 6个月
    0.749999997, # 9个月
    1,           # 1年
    2,           # 2年
    3,           # 3年
    4,           # 4年
    5,           # 5年
    6,           # 6年
    7,           # 7年
    8,           # 8年
    9,           # 9年
    10,          # 10年
    15,          # 15年
    20,          # 20年
    30           # 30年
]

# ============================================================================
# 目标期限定义（7个关键期限点）
# ============================================================================

TARGET_TERMS = [
    0.49999999799999995, # 0.5年（6个月）
    1.0,                 # 1年
    1.75,                # 1.75年（21个月）
    2.0,                 # 2年
    3.0,                 # 3年
    4.0,                 # 4年
    5.0                  # 5年
]

# 目标期限精度（0.01年 = 3.65天）
TARGET_TERM_PRECISION = 0.01

# 最大期限（年）
MAX_TERM_YEARS = 30.0

# ============================================================================
# 筛选条件配置
# ============================================================================

# 发行方式
ISSUANCE_METHODS = ['公募', '私募']

# 是否永续
IS_PERPETUAL = ['是', '否']

# 区域
REGIONS = ['ALL']

# 隐含评级
RATINGS = ['AA-', 'AA', 'AA+', 'AA(2)', 'AAA']

# 是否上市
LIST_STATUS = ['是', '否']

# ============================================================================
# 曲线类型配置
# ============================================================================

# 公募否否曲线（发行方式=公募，是否永续=否，是否次级=否）
CURVE_TYPE_PUBLIC_NON_PERPETUAL = {
    '发行方式1': '公募',
    '是否永续1': '否',
    '是否次级1': '否'
}

# 私募否否曲线（发行方式=私募，是否永续=否，是否次级=否）
CURVE_TYPE_PRIVATE_NON_PERPETUAL = {
    '发行方式1': '私募',
    '是否永续1': '否',
    '是否次级1': '否'
}

# 公募永续曲线（发行方式=公募，是否永续=是，是否次级=否）
CURVE_TYPE_PUBLIC_PERPETUAL = {
    '发行方式1': '公募',
    '是否永续1': '是',
    '是否次级1': '否'
}

# ============================================================================
# 输出配置
# ============================================================================

# 输出列名映射
OUTPUT_COLUMN_MAPPING = {
    '日期': 'DT',
    'trade_code': 'TRADE_CODE',
    '债券余额': 'balance',
    '隐含评级': 'imrating_calc',
    '标准化收益率': 'stdyield'
}

# 输出列顺序
OUTPUT_COLUMNS = ['DT', 'TRADE_CODE', 'balance', 'imrating_calc', 'stdyield']

# ============================================================================
# 数据修正规则
# ============================================================================

# 隐含评级与城投修正规则
DATA_CORRECTION_RULES = [
    {
        'condition': {'隐含评级': 'AA(2)', '是否城投': '否'},
        'correction': {'是否城投': '是'}
    },
    {
        'condition': {'隐含评级': 'AAA-', '是否城投': '是'},
        'correction': {'是否城投': '否'}
    }
]

# ============================================================================
# 日志配置
# ============================================================================

LOG_LEVEL = os.environ.get('LOG_LEVEL', 'INFO')
LOG_FORMAT = '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
