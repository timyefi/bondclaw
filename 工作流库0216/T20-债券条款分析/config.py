# -*- coding: utf-8 -*-
"""
债券条款分析配置文件
T20-债券条款分析

配置说明：
- 数据库连接信息通过环境变量获取
- 触发条件正则表达式模式可自定义扩展
"""

import os

# =============================================================================
# 数据库配置（使用环境变量）
# =============================================================================
DATABASE_CONFIG = {
    "host": os.environ.get("DB_HOST", "localhost"),
    "port": int(os.environ.get("DB_PORT", 3306)),
    "user": os.environ.get("DB_USER", "root"),
    "password": os.environ.get("DB_PASSWORD", ""),
    "database": os.environ.get("DB_NAME", "bond_data"),
}

# =============================================================================
# 数据文件配置
# =============================================================================
DATA_CONFIG = {
    # 输入文件路径（Excel格式）
    "input_excel_path": os.environ.get(
        "BOND_CLAUSE_INPUT_PATH",
        "data/带加速到期条款存续个券.xlsx"
    ),
    # 输出目录
    "output_dir": "output",
    # 输出文件名
    "output_json": "债券加速到期条款详细分析.json",
    "output_csv": "债券加速到期条款分析结果.csv",
    "output_excel": "带加速到期条款存续个券_分析结果.xlsx",
}

# =============================================================================
# 触发条件正则表达式模式
# =============================================================================
TRIGGER_PATTERNS = {
    "交叉违约": [
        r"交叉违约",
        r"合并范围内子公司.{0,50}出现违约",
        r"没有清偿到期应付.{0,30}贷款",
        r"其他.{0,20}债务.{0,30}违约",
        r"未能清偿到期应付",
        r"债务融资工具.{0,30}出现违约",
        r"半年内累计的总金额达到",
        r"金融机构贷款.*违约"
    ],
    "募投项目问题": [
        r"募投项目.{0,30}未开工",
        r"项目.{0,50}未收到.{0,10}服务费",
        r"项目.{0,20}提前终止",
        r"PPP项目.{0,10}提前终止",
        r"对债券偿还有重大影响的事项",
        r"产生重大不利影响的事项"
    ],
    "未能偿付本金/利息": [
        r"未能偿付.{0,20}本金",
        r"未能偿付.{0,20}利息",
        r"逾期",
        r"到期.{0,10}未能偿付",
        r"未能偿付.{0,20}到期应付"
    ],
    "违反契约承诺": [
        r"不履行或违反.{0,30}承诺",
        r"违反.{0,30}约定",
        r"不履行.{0,30}其他任何承诺",
        r"违反了约定",
        r"未履行承诺"
    ],
    "丧失清偿能力": [
        r"丧失清偿能力",
        r"破产",
        r"清算",
        r"被法院指定接管人",
        r"已开始相关的法律程序",
        r"进入破产程序"
    ],
    "重大资产变动": [
        r"出售.{0,10}重大资产",
        r"重大资产重组",
        r"转让重大资产",
        r"足以影响债券持有人利益的重大事项"
    ],
    "发生注销解散": [
        r"发生解散",
        r"注销",
        r"被吊销营业执照",
        r"停业",
        r"停产停业"
    ],
    "实际控制人变更": [
        r"实际控制人发生变更",
        r"控制权.{0,20}变更"
    ],
    "评级下调": [
        r"信用评级下调",
        r"债券信用评级下调"
    ],
    "财务指标恶化": [
        r"资产负债率超过\d+%",
        r"未偿还企业债券余额超过.{0,20}规模的\d+%"
    ]
}

# =============================================================================
# 投资者保护机制模式
# =============================================================================
PROTECTION_MECHANISM_PATTERNS = {
    "债券持有人会议": [
        r"债券持有人会议|债券持有人大会",
        r"召开.{0,20}债券持有人",
        r"持有人会议",
        r"持有人大会",
        r"债券持有人所持有的表决权",
        r"债券持有人所持表决权"
    ],
    "书面通知流程": [
        r"书面通知.{0,50}(发行人|债权代理人|投资人)",
        r"通知.{0,30}持有人",
        r"及时通知",
        r"(3|三)个工作日内.{0,20}书面通知",
        r"信息披露",
        r"\d+个(工作日|交易日)内进行信息披露"
    ],
    "宽限期": [
        r"宽限期|(\d+个?)(工作日|自然日|天)(内予以纠正|内补足)",
        r"予以(纠正|补足)完毕",
        r"(给予|同意给予).{0,30}宽限期",
        r"给予发行人.{0,20}(宽限期|工作日|天)"
    ],
    "增加担保": [
        r"增加担保|提供.{0,20}增信措施",
        r"担保.{0,30}增信",
        r"增加抵押/质押担保",
        r"增加抵质押",
        r"第三方担保",
        r"信用增进措施"
    ],
    "提前清偿部分/全部本金": [
        r"提前清偿.{0,10}(部分|全部).{0,10}本金",
        r"提前(偿还|偿付).{0,20}本金",
        r"本金视为立即到期",
        r"加速到期",
        r"立即到期应付",
        r"本金和利息视为立即到期"
    ]
}

# =============================================================================
# 其他债券特征模式
# =============================================================================
OTHER_FEATURE_PATTERNS = {
    "分期还本": [
        r"分次还本|分期偿还|分别偿还.{0,30}(\d+)%",
        r"提前偿付条款",
        r"本金提前偿付",
        r"偿付本金的\d+%",
        r"分期偿付",
        r"本金分年偿还",
        r"每年.{0,20}偿还本金",
        r"本金的\d+%、\d+%、\d+%"
    ],
    "利率调整权": [
        r"(上调|下调).{0,30}票面利率|决定.{0,20}调整.{0,20}票面利率",
        r"上调或下调.{0,10}基点",
        r"调整本期债券.{0,10}票面利率",
        r"利率调整选择权",
        r"票面利率选择权"
    ],
    "投资者回售权": [
        r"投资者有权选择.{0,50}回售|回售选择权",
        r"回售.{0,20}登记",
        r"回售给发行人",
        r"投资者回售登记期",
        r"选择将持有的.{0,20}债券.{0,20}回售"
    ],
    "弹性配售": [
        r"弹性配售",
        r"弹性配售选择权",
        r"基础发行额",
        r"当期计划发行规模",
        r"强制触发倍数"
    ]
}

# =============================================================================
# 分期还本比例模式
# =============================================================================
REPAYMENT_SCHEDULE_PATTERNS = [
    r'第(\d+)[、,，]第(\d+)[、,，]第(\d+)[、,，]第(\d+)[、,，]第(\d+)年[末尾]分别偿还[债券发行总额的]*(\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%',
    r'第(\d+)年末、第(\d+)年末、第(\d+)年末、第(\d+)年末、第(\d+)年末分别[按|偿还].*?(\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%',
    r'第(\d+).{0,5}第(\d+).{0,5}第(\d+).{0,5}第(\d+).{0,5}第(\d+)个计息年度末分别偿付本金的(\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%',
    r'分别偿还[债券发行总额的]*(\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%',
    r'分别按.*?(\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%[、,，](\d+)%',
    r'每年.{1,30}偿还本金。\d+-\d+年，分别偿还本金\d+万元',
    r'本金分年偿还.{1,50}分别偿还本金\d+万元'
]

# =============================================================================
# 交叉违约金额门槛模式
# =============================================================================
CROSS_DEFAULT_THRESHOLD_PATTERNS = [
    r'超过人民币([\d,，]+)万元',
    r'超过：.*?([\d,，]+)万元',
    r'达到或超过人民币([\d,，]+)万元',
    r'达到或超过.{0,5}([\d,，]+)万元',
    r'单独或累计的总金额达到([\d,，]+)万元',
    r'达到或超过：.*?([\d,，]+)万元'
]

# =============================================================================
# 可视化配置
# =============================================================================
VISUALIZATION_CONFIG = {
    # 图表颜色
    "colors": [
        "#1f77b4", "#ff7f0e", "#2ca02c", "#d62728", "#9467bd",
        "#8c564b", "#e377c2", "#7f7f7f", "#bcbd22", "#17becf"
    ],
    # 图表大小
    "figure_size": (12, 8),
    # 字体大小
    "title_fontsize": 14,
    "label_fontsize": 12,
    # 输出格式
    "output_format": "html",
    "offline_mode": True
}

# =============================================================================
# 分析参数配置
# =============================================================================
ANALYSIS_CONFIG = {
    # 每类触发条件最多保存的示例数量
    "max_examples_per_trigger": 3,
    # 每类保护机制最多保存的示例数量
    "max_examples_per_mechanism": 3,
    # 每类特征最多保存的示例数量
    "max_examples_per_feature": 3,
    # 分期还本模式最多保存的示例数量
    "max_examples_per_pattern": 2,
    # 交叉违约门槛最多保存的示例数量
    "max_examples_per_threshold": 2,
    # 上下文提取长度
    "context_length": 50
}
