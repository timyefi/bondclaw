# 联系方式面板

这一层描述桌面壳里的联系方式页面模型。

## 组成

- `lead_capture_model.py`：把联系方式策略、本地队列和投递状态组合成可渲染面板。
- 支持查看待重试数量，以及追加一条示例提交用于本地验证。

## 设计目标

- 首次核心动作时自然触发。
- 本地队列优先，网络投递并行。
- 只展示最小必要字段和回执状态。
- 不引入重型自建后端。

## 入口

桌面壳可以通过 `desktop-shell/launch_bondclaw.py --lead-capture-panel` 查看该面板的结构化数据。
