# 设置面板

This panel summarizes BondClaw branding, release channel, manifest URL, support links, and feature flags.

It is intended to be a first-class shell page so the future desktop UI can expose:

- Brand attribution
- Release channel and version
- Auto-update state
- Support entry points
- Feature flags

The panel reads from the runtime bridge and should stay in sync with the release manifest.
