分析下面需求，一定不要急着写代码，要先理解清楚需求，看看有没有需要与我讨论的。

计算TABLE（注意该表在edb库，一定要使用edb.edbdata） `edbdata` (
  `TRADE_CODE` varchar(255) NOT NULL,
  `DT` date NOT NULL,
  `CLOSE` double DEFAULT NULL,
  `modify_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`TRADE_CODE`,`DT`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8;

中：

TRADE_CODE=CRB CMDT Index 的close

/

1/2 （TRADE_CODE=FLOT US Equity 的close +TRADE_CODE=.EARNREV G Index 的close）

dt>=2020-11-12

设置起点为2020-11-12，起点值设定为0.09427710843373494，以此来调整之后的值，输出dt,close列（调整为周度频率即可）
插入到 全球实物资产比金融资产 表中。注意dt不要重复。


数据库连接：sql_engine = sqlalchemy.create_engine(
    'mysql+pymysql://%s:%s@%s:%s/%s' % (
        'hz_work',
        'Hzinsights2015',
        'rm-uf6c8yi6zdq6ea7p1qo.mysql.rds.aliyuncs.com',
        '3306',
        'yq',
    ), poolclass=sqlalchemy.pool.NullPool
)
_cursor = sql_engine.connect()