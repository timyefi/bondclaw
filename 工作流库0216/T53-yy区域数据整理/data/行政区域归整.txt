insert IGNORE into xzqh_yy (区域,省,市,区县)
SELECT 
		DISTINCT
    `Administrative Division Name`,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    '省直辖县级行政区划' AS 市,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 区县
FROM yyqy
WHERE 
(`Administrative Division Name` LIKE '%五家渠市%' 
    OR `Administrative Division Name` LIKE '%北屯市%'
    OR `Administrative Division Name` LIKE '%双河市%'
    OR `Administrative Division Name` LIKE '%可克达拉市%'
    OR `Administrative Division Name` LIKE '%图木舒克市%'
    OR `Administrative Division Name` LIKE '%新星市%'
    OR `Administrative Division Name` LIKE '%昆玉市%'
    OR `Administrative Division Name` LIKE '%石河子市%'
    OR `Administrative Division Name` LIKE '%胡杨河市%'
    OR `Administrative Division Name` LIKE '%铁门关市%'
    OR `Administrative Division Name` LIKE '%阿拉尔市%'
    OR `Administrative Division Name` LIKE '%济源市%'
    OR `Administrative Division Name` LIKE '%万宁市%'
    OR `Administrative Division Name` LIKE '%东方市%'
    OR `Administrative Division Name` LIKE '%临高县%'
    OR `Administrative Division Name` LIKE '%乐东黎族自治县%'
    OR `Administrative Division Name` LIKE '%五指山市%'
    OR `Administrative Division Name` LIKE '%保亭黎族苗族自治县%'
    OR `Administrative Division Name` LIKE '%定安县%'
    OR `Administrative Division Name` LIKE '%屯昌县%'
    OR `Administrative Division Name` LIKE '%文昌市%'
    OR `Administrative Division Name` LIKE '%昌江黎族自治县%'
    OR `Administrative Division Name` LIKE '%澄迈县%'
    OR `Administrative Division Name` LIKE '%琼中黎族苗族自治县%'
    OR `Administrative Division Name` LIKE '%琼海市%'
    OR `Administrative Division Name` LIKE '%白沙黎族自治县%'
    OR `Administrative Division Name` LIKE '%陵水黎族自治县%'
    OR `Administrative Division Name` LIKE '%仙桃市%'
    OR `Administrative Division Name` LIKE '%天门市%'
    OR `Administrative Division Name` LIKE '%潜江市%'
    OR `Administrative Division Name` LIKE '%神农架林区%')

insert IGNORE into xzqh_yy (区域,省,市,区县)
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    '省本级' AS 市,
    '' AS 区县
FROM yyqy
WHERE `Administrative Level`='Development Of Provincial'
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    '' AS 市,
    '' AS 区县
FROM yyqy
WHERE `Administrative Level`='Provincial'
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 市,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', -1) AS 区县
FROM yyqy
WHERE `Administrative Level`='New District Of National'
AND (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 市,
    '市本级' AS 区县
FROM yyqy
WHERE `Administrative Level`='New District Of National'
AND NOT (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 市,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', -1) AS 区县
FROM yyqy
WHERE `Administrative Level`='District'
AND (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 市,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', -1) AS 区县
FROM yyqy
WHERE `Administrative Level`='District'
AND not (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 市,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 区县
FROM yyqy
WHERE (`Administrative Level`='City' or `Administrative Level`='Development Of District' or `Administrative Level`='Development Of City')
AND (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 市,
    '' AS 区县
FROM yyqy
WHERE (`Administrative Level`='City' or `Administrative Level`='Development Of City')
AND not (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 市,
    '市本级' AS 区县
FROM yyqy
WHERE ( `Administrative Level`='Development Of City')
AND not (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
union all
SELECT 
		DISTINCT
    `Administrative Division Name` as 区域,
    SUBSTRING_INDEX(`Administrative Division Name`, '-', 1) AS 省,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 2),'-',-1) AS 市,
    SUBSTRING_INDEX(SUBSTRING_INDEX(`Administrative Division Name`, '-', 3),'-',-1) AS 区县
FROM yyqy
WHERE (`Administrative Level`='Development Of District')
AND not (`Administrative Division Name` LIKE '北京市-%' 
    OR `Administrative Division Name` LIKE '上海市-%'
    OR `Administrative Division Name` LIKE '天津市-%'
    OR `Administrative Division Name` LIKE '重庆市-%')
		


		
汇川区
滨海新区
新吴区（无锡高新区）		
北仑区（宁波经济技术开发区）	
昭山示范区


SELECT distinct `Administrative Level`
from yyqy

SELECT distinct 区县
from bond.fiscal_region_city_county
WHERE 市='省直辖县级行政区划'
		
	National

Development Of District

City
Development Of City


